# Programming language
language: c
# Build only master branch
branches:
  only:
  - master
  - release
# Install Arduino packagage
before_install:
  - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_1.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :1 -ac -screen 0 1280x1024x16"
  - sleep 3
  - export DISPLAY=:1.0
  - wget http://downloads.arduino.cc/arduino-1.8.10-linux64.tar.xz
  - tar xf arduino-1.8.10-linux64.tar.xz
  - sudo mv arduino-1.8.10 /usr/local/share/arduino
  - sudo ln -s /usr/local/share/arduino/arduino /usr/local/bin/arduino
#install library's
install:
  #- ln -s $PWD /usr/local/share/arduino/libraries/Adafruit_FONA
  #- arduino --install-library "Adafruit SleepyDog Library,Adafruit MQTT Library"
## Update composer and run the appropriate composer command
before_script:
  - composer self-update -q
  - if [ -n "$PUBLIC_KEY" ]; then composer config github-oauth.github.com ${PUBLIC_KEY}; fi;
  - if [ -z "$dependencies" ]; then composer install; fi;
  - if [ "$dependencies" = "lowest" ]; then composer update --prefer-lowest -n; fi;
  - if [ "$dependencies" = "highest" ]; then composer update -n; fi;
  - composer show -i
script:
  - arduino --verify --board arduino:avr:uno $PWD/TravisCIblink.ino
#
before_deploy:
# Set up git user name and tag this commit
  #- git config --local user.name "MrBuren121"
  #- git config --local user.email "YOUR GIT USER EMAIL"
  - export TRAVIS_TAG=${TRAVIS_TAG:-$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)}
  - git tag $TRAVIS_TAG
deploy:
  provider: releases
  api_key: PUBLIC_KEY
  #file: TravisCIblink.ino
  skip_cleanup: true
notifications:
  #email:
    #recipients:
      #- 
    #on_success: always
    #on_failure: always
